# Python
.venv/
__pycache__/
*.pyc

# Model weights (auto-download via ultralytics)
*.pt

# Generated data & outputs (regenerable); keep only the comparison images
data/
out/*
!out/compare_*.png

# Local tooling (gh CLI downloaded for pushing)
.tools/

# KITTI dataset (~15 GB, downloaded locally for real-sequence testing)
kitti/
